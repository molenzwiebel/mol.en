extern CFile {
    fn fopen(filename: String, mode: String) -> *Void
    fn fputs(contents: String, file: *Void) -> Int
    fn fclose(file: *Void) -> Int
}

file_ptr = CFile.fopen("my_file.txt", "ab+")
CFile.fputs("Hello, world!", file_ptr)
CFile.fclose(file_ptr)
